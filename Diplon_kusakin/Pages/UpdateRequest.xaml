<Page x:Class="Diplon_kusakin.Pages.UpdateRequest"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Diplon_kusakin.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UpdateRequest">

    <Grid Background="AliceBlue">
        <Grid.RowDefinitions>

            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>


        </Grid.RowDefinitions>
        <Grid Background="#008080">
            <TextBlock Grid.Row="0" Text="Измененение описания заявки" FontSize="20" FontWeight="Bold" Margin="5" Foreground="White"/>
            <Button Grid.Row="0" Content="Назад"  Width="100" Height="30" Background="White" Foreground="Black" FontWeight="Bold" FontSize="14" BorderThickness="0" HorizontalAlignment="Right" Click="Button_Click" Margin="10">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="15">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="White"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="False">
                                            <Setter TargetName="border" Property="Background" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <StackPanel Grid.Row="1"  Margin="10">
            <TextBlock Text="Оборудование:" Margin="0 0 10 0" FontSize="16"/>
            <TextBox x:Name="txtEquipment" Height="31" Margin="0" FontSize="16" BorderThickness="0,0,0,1.5"  />
        </StackPanel>
        <StackPanel Grid.Row="2"  Margin="10">
            <TextBlock Text="Код Ошибки:" Margin="0 0 10 0" FontSize="16"/>
            <TextBox x:Name="txtKabinet" Height="31" Margin="0" FontSize="16" BorderThickness="0,0,0,1.5"  />
        </StackPanel>

        <StackPanel Grid.Row="3"  Margin="10">
            <TextBlock Text="Тип неисправности:" Margin="0 0 10 0" FontSize="16"/>

            <ComboBox x:Name="txtFaultType" Height="31" Margin="0" FontSize="16" BorderThickness="0,0,0,1.5">
                <ComboBoxItem Content="Механическая неисправность"/>
                <ComboBoxItem Content="Электрическая неисправность"/>
                <ComboBoxItem Content="Программная неисправность"/>
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="4" Margin="10">
            <TextBlock Text="Описание проблемы:" Margin="0 0 0 5" FontSize="16"/>
            <TextBox x:Name="txtDescription" Height="100" Margin="0" FontSize="16" />
        </StackPanel>

        <StackPanel Grid.Row="5" Margin="10"/>
        <Button  Content="Изменить заявку" Click="btnSave_Click" Background="#1268b3" Foreground="White" FontWeight="Bold" FontSize="14" BorderThickness="0" Margin="313,141,327,-30" Grid.Row="4" >
            <Button.Style>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type Button}">
                                <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="15">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#1268b3"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="False">
                                        <Setter TargetName="border" Property="Background" Value="#1268b3"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Page>
